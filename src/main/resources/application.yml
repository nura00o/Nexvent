server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Nexvent
    username: postgres
    password: 1234 # на свой пароль в постгре поменяй
    sql:
      init:
        mode: always
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USER:nurakhmed777@gmail.com}
    password: ${MAIL_PASS:ngcrilehifmcraol}
    properties.mail.smtp:
      auth: true
      starttls.enable: true
  management:
    health:
      mail:
        enabled: false

app:
  jwt:
    secret: ${JWT_SECRET:fzSTpXn0veeBDovlq0ntHpXbPQG3LUZ13lsFUqZNEFEP5qmcdhED7Vy7LEt2weQh}
    accessExpirationMs: 900000
    refreshExpirationMs: 604800000
